--extra-index-url https://download.pytorch.org/whl/cu118

demucs==4.0.1
torch==2.1.2
torchaudio==2.1.2
numpy==1.26.4
librosa==0.10.2
soundfile==0.12.1
pydub==0.25.1
inaSpeechSegmenter==0.7.8
# tensorflow bundles its own keras, so the security scanner's "vulnerable keras"
# finding has to be fixed by bumping tensorflow itself, not pinning keras
# separately (which would conflict with tensorflow's bundled copy)
tensorflow==2.21.0
